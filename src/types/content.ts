export type NavItem = {
  label: string;
  href: string;
};

export type Service = {
  title: string;
  description: string;
};

export type Differential = {
  title: string;
  description: string;
};

export type ContactCard = {
  title: string;
  detail: string;
  helper: string;
};

export type Review = {
  name: string;
  rating: string;
  text: string;
  avatar?: string;
};
